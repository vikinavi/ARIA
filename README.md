# AI Agent with RAG, Extendable Memory & Database

## 🚀 Setup
```bash
pip install -r requirements.txt
uvicorn src.main:app --reload
```

## 🐳 Docker
```bash
docker-compose up --build
```

API available at: `http://localhost:8000/docs`
